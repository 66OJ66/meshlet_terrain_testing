use bevy::prelude::*;

/// Marker component for the Player's body.
#[derive(Component)]
pub struct Player;

/// Marker component for the Player's camera.
#[derive(Component)]
pub struct PlayerCamera;
